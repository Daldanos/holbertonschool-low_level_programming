#include "main.h"
#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* _abs - absolute value
* @r: int variable
* Return: n if positive, n * -1 if negative
*/
int _abs(int r)
{
	if (r >= 0)
		return (n);
	else
		return (n * (-1));
}
