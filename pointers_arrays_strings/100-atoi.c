#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _atoi - main function
 * @s: pointer variable
 * Return: a
 */
int _atoi(char *s)
{
	int a;
	a = _atoi(s);
	printf("%d\n", a);
	return (0);
}
