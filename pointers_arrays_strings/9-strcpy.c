#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * puts_half - main function
 * @str: pointer variable
 *
 */
char *_strcpy(char *dest, char *src)
{
	*_strcpy(dest, src);
	return (dest);
}
